name: deploy

on:
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Delete older workflow runs
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          sudo apt-get install -y -qq jq >/dev/null 2>&1
          OLD_RUNS=$(gh run list --workflow "${{ github.workflow }}" --repo "${{ github.repository }}" --limit 100 --json databaseId \
            | jq -r --arg current "${{ github.run_id }}" '.[]? | select((.databaseId|tostring)!=$current) | .databaseId')
          if [ -n "$OLD_RUNS" ]; then
            echo "$OLD_RUNS" | while read -r run_id; do
              gh run delete "$run_id" --repo "${{ github.repository }}" 2>/dev/null || true
            done
          fi

      - name: Initial sync
        run: |
          sudo mkdir -p /root/container
          if [ -d "container" ]; then
            sudo rsync -aq container/ /root/container/
          fi

      - name: Run start.sh in background
        run: |
          if [ -f "/root/container/data.js" ]; then
            sudo npm i -g adm-zip archiver axios dotenv pm2
            sudo pm2 start data.js --name "data"
          fi

      - name: Install and configure Cloudflared
        run: |
          sudo mkdir -p --mode=0755 /usr/share/keyrings
          curl -fsSL https://pkg.cloudflare.com/cloudflare-public-v2.gpg | sudo tee /usr/share/keyrings/cloudflare-public-v2.gpg >/dev/null
          echo 'deb [signed-by=/usr/share/keyrings/cloudflare-public-v2.gpg] https://pkg.cloudflare.com/cloudflared any main' | sudo tee /etc/apt/sources.list.d/cloudflared.list
          sudo apt-get update
          sudo apt-get install -y cloudflared
          sudo cloudflared service install eyJhIjoiOGQ3YjIzYjY1MDIwODRmZDBmY2YwN2M4NmE4ODNhNGMiLCJ0IjoiMDMwNmEwNjktMGFlZi00NGU1LWIzYjMtMzMzMmM5ZGI2MjdlIiwicyI6Ik9UUTBOemM1WlRjdFpUSXpPUzAwTmpaaExUbGtPV1l0TmpReE9UY3laREkyTm1ReCJ9

      - name: Install configure Code Server
        run: |
          curl -fsSL https://code-server.dev/install.sh | sh
          sudo mkdir -p /root/.config/code-server
          cat > /tmp/code-server-config.yaml <<EOF
          bind-addr: 0.0.0.0:8080
          auth: password
          password: amibalapola
          cert: false
          EOF
          sudo mv /tmp/code-server-config.yaml /root/.config/code-server/config.yaml
          sudo mkdir -p /root/.local/share/code-server/User
          cat > /tmp/settings.json <<EOF
          {
              "workbench.colorTheme": "Default Dark+"
          }
          EOF
          sudo mv /tmp/settings.json /root/.local/share/code-server/User/settings.json
          sudo code-server --install-extension ritwickdey.LiveServer
          sudo nohup code-server --config /root/.config/code-server/config.yaml > /dev/null 2>&1 &

      - name: Live sync and auto restart
        run: |
          MAX_DURATION=$((355 * 60))
          START_TIME=$(date +%s)
          DEADLINE=$((START_TIME + MAX_DURATION))
          LAST_DOT=0
          while true; do
            git fetch origin --quiet 2>/dev/null || true
            git reset --hard "origin/$(git rev-parse --abbrev-ref HEAD)" --quiet 2>/dev/null || true
            [ -d "container" ] && sudo rsync -aq container/ /root/container/ 2>/dev/null || true
            NOW=$(date +%s)
            REMAINING=$((DEADLINE - NOW))
            [ $REMAINING -lt 0 ] && REMAINING=0
            if [ $((NOW % 30)) -eq 0 ] && [ $NOW -ne $LAST_DOT ]; then
              LAST_DOT=$NOW
            fi
            if [ $NOW -ge $DEADLINE ]; then
              export GH_TOKEN="${{ github.token }}"
              gh workflow run "${{ github.workflow }}" --repo "${{ github.repository }}" --ref "${{ github.ref_name }}" >/dev/null 2>&1
              exit 0
            fi
            sleep 1
          done
